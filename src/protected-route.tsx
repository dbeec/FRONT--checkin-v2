// import { Navigate, Outlet } from "react-router-dom";

// export default function Protected({ children, redirectTo= "/" }: { children: any, redirectTo: any }) {
//   if (!user) {
//     return <Navigate to={redirectTo}/>
//   }
//   return {children} ? {children} : <Outlet />
// }
